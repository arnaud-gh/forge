// Placeholder root. The design system (step 2), primitives (step 3),
// auth (step 5), PWA (step 6) and the tab shell (step 7) are layered on in later M0 steps.
export function App() {
  return (
    <main
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        background: '#0B0D10',
        color: '#E6ECF2',
        fontFamily: 'system-ui, sans-serif',
      }}
    >
      <p>Forge — scaffold ready</p>
    </main>
  );
}
